// Shared signing and deployment settings.
// Copy Config/Local.xcconfig.example to Config/Local.xcconfig and set DEVELOPMENT_TEAM for device signing.

#include? "Local.xcconfig"

CODE_SIGN_IDENTITY = Apple Development
MACOSX_DEPLOYMENT_TARGET = 12.0
ARCHS = $(ARCHS_STANDARD)
